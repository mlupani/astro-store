---

interface Props {
    src: string;
    alt: string;
    className: string;
}

const { alt, src, className } = Astro.props

const imageSrc = src.startsWith('http') ? src : `${import.meta.env.SITE_URL}/images/products/${src}`


---


<img src={imageSrc} alt={alt} class:list={[className]}>
